import 'package:flutter/material.dart';
import 'package:hello_world/models/post.dart';

class PostDetailItem extends StatelessWidget {
  final Post _post;

  PostDetailItem(this._post);

  @override
  Widget build(BuildContext context) {
    return new Card();
  }
}
